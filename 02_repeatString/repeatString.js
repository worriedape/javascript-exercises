const repeatString = (string, num) => {
  let wordWord = " ";

  for (let i = 0; i < num; i++) {
    wordWord += `${string}`;
  }
  return wordWord;
};

// Do not edit below this line
module.exports = repeatString;
